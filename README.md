# jarvis

functionality
* add Plugin
* execute Plugin(command)
* export Plugin
* import Plugin

## add Plugin
![addPlugin](./assets/images/createPlugin.gif)


## to do:
1. 参数类型添加可变参数和参数支持默认值设置
2. 正则表达式增加匹配次数设置（1次/10次/循环匹配）
3. 导出可选支持将可本地执行文件和配置一起打包，导入同样支持导入压缩包
